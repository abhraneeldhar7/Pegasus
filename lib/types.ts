export interface studentType {
    student_id: number,
    name: string,
    email: string,
    department: string,
    dept_id: number,
    semester: number
}

export interface departmentType {
    department_id: number,
    name: string,
    code: string
}